import React from "react";

const Header = () => {
    return (
        <div className="bg-gray-400 py-4 hover:text-yellow-500 cursor-pointer">
            <h1 className="text-xl font-bold">Responsive Sliding Cards in HTML CSS JS</h1>
        </div>
    )
}

export default Header;